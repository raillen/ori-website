---
title: "ori.random"
description: "Pseudorandom number generation (PRNG) routines for generating floats and integers within ranges."
---

Pseudorandom number generation (PRNG) routines for generating floats and integers within ranges.

### `bool`

```ori
() -> bool
```

*Layer 1 · runtime* · **Aliases:** `random.bool`

### `choice`

```ori
(arg0: list<_#0>) -> optional<_#0>
```

*Layer 1 · runtime* · **Aliases:** `random.choice`

### `float`

```ori
(arg0: float, arg1: float) -> float
```

*Layer 1 · runtime* · **Aliases:** `random.float`

Generates a pseudorandom floating-point number between 0.0 and 1.0.

* **Applicability:** Useful for generating percentages, probability weights, random noise multipliers, and easing variations.

```ori
// Example
import ori.random as rnd
import ori.io as io

func main()
    const weight = rnd.float()
    io.println(string(weight)) // Prints random float between 0.0 and 1.0
end
```

### `int`

```ori
(arg0: int, arg1: int) -> int
```

*Layer 1 · runtime* · **Aliases:** `random.int`

### `seed`

```ori
(arg0: int) -> void
```

*Layer 1 · runtime* · **Aliases:** `random.seed`

### `shuffle`

```ori
(arg0: list<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `random.shuffle`

