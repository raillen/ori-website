---
title: "ori.path"
description: "File system path parser. Allows joining segments, resolving absolute paths, and extracting filenames or extensions."
---

File system path parser. Allows joining segments, resolving absolute paths, and extracting filenames or extensions.

### `absolute`

```ori
func absolute(value: string, base: string) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `base_name`

```ori
func base_name(value: string) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `change_extension`

```ori
func change_extension(value: string, new_ext: string) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `extension`

```ori
func extension(value: string) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `has_extension`

```ori
func has_extension(value: string, expected: string) -> bool
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `is_absolute`

```ori
func is_absolute(value: string) -> bool
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `is_relative`

```ori
func is_relative(value: string) -> bool
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `join`

```ori
func join(parts: list<string>) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

Combines list segments into a unified file system path, automatically resolving separator slashes.

* **Applicability:** Used to dynamically build local paths cross-platform without worrying about '/' vs '\' differences.

```ori
// Example
import ori.path as path
import ori.io as io

func main()
    const target = path.join("docs", "spec", "errors.md")
    io.println(target) // Prints "docs/spec/errors.md" (or system equivalent)
end
```

### `name_without_extension`

```ori
func name_without_extension(value: string) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `normalize`

```ori
func normalize(value: string) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `parent`

```ori
func parent(value: string) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

### `relative`

```ori
func relative(value: string, from: string) -> string
```

*Layer 2 · .orl* · **Source:** `path.orl`

