---
title: "ori"
description: "Core language runtime module. Provides fundamental runtime operations."
---

Core language runtime module. Provides fundamental runtime operations.

### `panic`

```ori
(arg0: string) -> never
```

*Layer 1 · runtime*

Terminates the program with a panic message.

