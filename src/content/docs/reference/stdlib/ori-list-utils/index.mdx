---
title: "ori.list.utils"
description: "List utility helpers. Provides convenience accessors with fallback defaults."
---

List utility helpers. Provides convenience accessors with fallback defaults.

### `first_or`

```ori
func first_or(items: list<T>, fallback: T) -> T
```

*Layer 2 · .orl* · **Source:** `list/utils.orl`

Performs the first or operation on utils.

### `get_or`

```ori
func get_or(items: list<T>, index: int, fallback: T) -> T
```

*Layer 2 · .orl* · **Source:** `list/utils.orl`

Performs the get or operation on utils.

### `last_or`

```ori
func last_or(items: list<T>, fallback: T) -> T
```

*Layer 2 · .orl* · **Source:** `list/utils.orl`

Performs the last or operation on utils.

### `singleton`

```ori
func singleton(item: T) -> list<T>
```

*Layer 2 · .orl* · **Source:** `list/utils.orl`

Performs the singleton operation on utils.

