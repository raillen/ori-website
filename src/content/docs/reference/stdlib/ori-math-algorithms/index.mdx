---
title: "ori.math.algorithms"
description: "Mathematical algorithms. Provides advanced numeric operations like clamping, interpolation, and approximation."
---

Mathematical algorithms. Provides advanced numeric operations like clamping, interpolation, and approximation.

### `clamp_float`

```ori
func clamp_float(value: float, min_val: float, max_val: float) -> float
```

*Layer 2 · .orl* · **Source:** `math/algorithms.orl`

Performs the clamp float operation on algorithms.

### `hypot`

```ori
func hypot(a: float, b: float) -> float
```

*Layer 2 · .orl* · **Source:** `math/algorithms.orl`

Performs the hypot operation on algorithms.

### `is_approx_zero`

```ori
func is_approx_zero(value: float) -> bool
```

*Layer 2 · .orl* · **Source:** `math/algorithms.orl`

Performs the is approx zero operation on algorithms.

### `lerp_float`

```ori
func lerp_float(a: float, b: float, t: float) -> float
```

*Layer 2 · .orl* · **Source:** `math/algorithms.orl`

Performs the lerp float operation on algorithms.

### `sign_float`

```ori
func sign_float(value: float) -> int
```

*Layer 2 · .orl* · **Source:** `math/algorithms.orl`

Performs the sign float operation on algorithms.

