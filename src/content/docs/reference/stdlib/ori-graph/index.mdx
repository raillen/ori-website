---
title: "ori.graph"
description: "Graph data structure. Supports directed and weighted graphs with BFS, DFS, shortest path, topological sort, and cycle detection."
---

Graph data structure. Supports directed and weighted graphs with BFS, DFS, shortest path, topological sort, and cycle detection.

### `add_edge`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0, arg2: _#0) -> void
```

*Layer 1 · runtime* · **Aliases:** `graph.add_edge`

Adds a directed edge between two nodes.

### `add_node`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0) -> void
```

*Layer 1 · runtime* · **Aliases:** `graph.add_node`

Adds a new node to the graph.

### `add_weighted_edge`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0, arg2: _#0, arg3: int) -> void
```

*Layer 1 · runtime* · **Aliases:** `graph.add_weighted_edge`

Adds a weighted directed edge between two nodes.

### `bfs`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `graph.bfs`

Performs a breadth-first traversal starting from the given node.

### `clone`

```ori
(arg0: graph.Graph<_#0>) -> graph.Graph<_#0>
```

*Layer 1 · runtime* · **Aliases:** `graph.clone`

Returns a deep copy of the collection, independent of the original.

### `components`

```ori
(arg0: graph.Graph<_#0>) -> list<list<_#0>>
```

*Layer 1 · runtime* · **Aliases:** `graph.components`

Returns the connected components of the graph.

### `dfs`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `graph.dfs`

Performs a depth-first traversal starting from the given node.

### `edge_len`

```ori
(arg0: graph.Graph<_#0>) -> int
```

*Layer 1 · runtime* · **Aliases:** `graph.edge_len`

Returns the number of edges in the graph.

### `edge_weight`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0, arg2: _#0) -> optional<int>
```

*Layer 1 · runtime* · **Aliases:** `graph.edge_weight`

Returns the weight of the edge between two nodes.

### `edges`

```ori
(arg0: graph.Graph<_#0>) -> list<tuple<_#0, _#0>>
```

*Layer 1 · runtime* · **Aliases:** `graph.edges`

Returns all edges in the graph as source-target pairs.

### `has_cycle`

```ori
(arg0: graph.Graph<_#0>) -> bool
```

*Layer 1 · runtime* · **Aliases:** `graph.has_cycle`

Returns true if the graph contains at least one cycle.

### `has_edge`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0, arg2: _#0) -> bool
```

*Layer 1 · runtime* · **Aliases:** `graph.has_edge`

Returns true if a direct edge exists between the two nodes.

### `has_node`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0) -> bool
```

*Layer 1 · runtime* · **Aliases:** `graph.has_node`

Returns true if the graph contains the specified node.

### `is_directed`

```ori
(arg0: graph.Graph<_#0>) -> bool
```

*Layer 1 · runtime* · **Aliases:** `graph.is_directed`

Returns true if the graph is directed.

### `len`

```ori
(arg0: graph.Graph<_#0>) -> int
```

*Layer 1 · runtime* · **Aliases:** `graph.len`

Returns the number of elements in the collection.

### `neighbors`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `graph.neighbors`

Returns the list of nodes directly reachable from the given node.

### `new`

```ori
(arg0: bool) -> graph.Graph<_#0>
```

*Layer 1 · runtime* · **Aliases:** `graph.new`

Creates a new empty instance of the graph collection.

### `nodes`

```ori
(arg0: graph.Graph<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `graph.nodes`

Returns all nodes in the graph.

### `remove_edge`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0, arg2: _#0) -> void
```

*Layer 1 · runtime* · **Aliases:** `graph.remove_edge`

Removes the edge between two nodes.

### `remove_node`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0) -> void
```

*Layer 1 · runtime* · **Aliases:** `graph.remove_node`

Removes a node and all its connected edges from the graph.

### `shortest_path`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0, arg2: _#0) -> optional<list<_#0>>
```

*Layer 1 · runtime* · **Aliases:** `graph.shortest_path`

Computes the shortest unweighted path between two nodes.

### `shortest_weighted_path`

```ori
(arg0: graph.Graph<_#0>, arg1: _#0, arg2: _#0) -> optional<list<_#0>>
```

*Layer 1 · runtime* · **Aliases:** `graph.shortest_weighted_path`

Computes the shortest weighted path between two nodes using Dijkstra's algorithm.

### `strongly_connected_components`

```ori
(arg0: graph.Graph<_#0>) -> list<list<_#0>>
```

*Layer 1 · runtime* · **Aliases:** `graph.strongly_connected_components`

Finds all strongly connected components in the graph.

### `topological_sort`

```ori
(arg0: graph.Graph<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `graph.topological_sort`

Returns a topological ordering of the graph nodes.

### `transitive_closure`

```ori
(arg0: graph.Graph<_#0>) -> graph.Graph<_#0>
```

*Layer 1 · runtime* · **Aliases:** `graph.transitive_closure`

Computes the transitive closure — for each pair (A, B), stores whether B is reachable from A.

### `try_topological_sort`

```ori
(arg0: graph.Graph<_#0>) -> optional<list<_#0>>
```

*Layer 1 · runtime* · **Aliases:** `graph.try_topological_sort`

Attempts a topological sort, returning an error if the graph has cycles.

