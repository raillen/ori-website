---
title: "ori.format.utils"
description: "Format utility helpers. Provides additional formatting functions with custom parameters."
---

Format utility helpers. Provides additional formatting functions with custom parameters.

### `binary`

```ori
func binary(value: int) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Formats a number as a binary (base-2) string.

### `bytes_size`

```ori
func bytes_size(value: int) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Formats a byte count as a human-readable size string (e.g., "1.5 MB").

### `bytes_size_unit`

```ori
func bytes_size_unit(value: int, unit: string) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Performs the bytes size unit operation on utils.

### `date`

```ori
func date(ts: int, pattern: string) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Formats a timestamp as a date string.

### `datetime`

```ori
func datetime(ts: int, date_pattern: string, time_pattern: string) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Formats a timestamp as a date-time string.

### `hex`

```ori
func hex(value: int) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Formats a number as a hexadecimal string.

### `number`

```ori
func number(value: float) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Formats a number with locale-aware separators.

### `number_int`

```ori
func number_int(value: int) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Performs the number int operation on utils.

### `number_with`

```ori
func number_with(value: float, decimals: int) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Performs the number with operation on utils.

### `percent`

```ori
func percent(value: float) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Formats a float as a percentage string.

### `percent_with`

```ori
func percent_with(value: float, decimals: int) -> string
```

*Layer 2 · .orl* · **Source:** `format/utils.orl`

Performs the percent with operation on utils.

