---
title: "ori.map.algorithms"
description: "Map algorithms. Provides batch operations like merging, inversion, key counting, and value aggregation on typed maps."
---

Map algorithms. Provides batch operations like merging, inversion, key counting, and value aggregation on typed maps.

### `has_all_keys`

```ori
func has_all_keys(m: map<string, int>, keys: list<string>) -> bool
```

*Layer 2 · .orl* · **Source:** `map/algorithms.orl`

Performs the has all keys operation on algorithms.

### `invert_string_int`

```ori
func invert_string_int(m: map<string, int>) -> map<int, string>
```

*Layer 2 · .orl* · **Source:** `map/algorithms.orl`

Performs the invert string int operation on algorithms.

### `key_count_string_int`

```ori
func key_count_string_int(m: map<string, int>) -> int
```

*Layer 2 · .orl* · **Source:** `map/algorithms.orl`

Performs the key count string int operation on algorithms.

### `merge_string_int`

```ori
func merge_string_int(base: map<string, int>, overlay: map<string, int>) -> map<string, int>
```

*Layer 2 · .orl* · **Source:** `map/algorithms.orl`

Performs the merge string int operation on algorithms.

### `values_sum_int`

```ori
func values_sum_int(m: map<string, int>) -> int
```

*Layer 2 · .orl* · **Source:** `map/algorithms.orl`

Performs the values sum int operation on algorithms.

