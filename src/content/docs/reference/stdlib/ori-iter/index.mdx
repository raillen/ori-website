---
title: "ori.iter"
description: "Iterator functional operations. Provides map, filter, reduce, sort, and other higher-order functions for sequences."
---

Iterator functional operations. Provides map, filter, reduce, sort, and other higher-order functions for sequences.

### `all`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> bool) -> bool
```

*Layer 1 · runtime* · **Aliases:** `iter.all`

Returns true if all elements satisfy the given predicate.

### `any`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> bool) -> bool
```

*Layer 1 · runtime* · **Aliases:** `iter.any`

Returns true if at least one element satisfies the predicate.

### `count_where`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> bool) -> int
```

*Layer 1 · runtime* · **Aliases:** `iter.count_where`

Returns the number of elements that satisfy the predicate.

### `filter`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> bool) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.filter`

Returns a new collection containing only elements that satisfy the given predicate.

### `find`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> bool) -> optional<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.find`

Searches for a node matching the predicate and returns its value.

### `flat_map`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> list<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.flat_map`

Applies a function that returns a list to each element, then flattens the results.

### `flatten`

```ori
(arg0: list<list<_#0>>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.flatten`

Flattens a list of lists into a single list.

### `group_by`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> _#0) -> map<_#0, list<_#0>>
```

*Layer 1 · runtime* · **Aliases:** `iter.group_by`

Groups elements by a key function into a map of lists.

### `map`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> _#0) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.map`

Returns a new collection by applying a transformation function to each element.

### `partition`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> bool) -> tuple<list<_#0>, list<_#0>>
```

*Layer 1 · runtime* · **Aliases:** `iter.partition`

Splits elements into two groups based on a predicate.

### `reduce`

```ori
(arg0: list<_#0>, arg1: _#0, arg2: func(_#0, _#0) -> _#0) -> _#0
```

*Layer 1 · runtime* · **Aliases:** `iter.reduce`

Reduces all elements to a single value using an accumulator function.

### `reverse`

```ori
(arg0: list<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.reverse`

Returns a new collection with elements in reversed order.

### `skip`

```ori
(arg0: list<_#0>, arg1: int) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.skip`

Skips the current test.

### `sort`

```ori
(arg0: list<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.sort`

Returns a new collection with elements sorted in ascending order.

### `sort_by`

```ori
(arg0: list<_#0>, arg1: func(_#0, _#0) -> int) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.sort_by`

Returns a new collection with elements sorted by the given key function.

### `take`

```ori
(arg0: list<_#0>, arg1: int) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.take`

Returns a new collection with at most N elements from the start.

### `unique`

```ori
(arg0: list<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `iter.unique`

Returns a new collection with duplicate elements removed.

### `zip`

```ori
(arg0: list<_#0>, arg1: list<_#0>) -> list<tuple<_#0, _#0>>
```

*Layer 1 · runtime* · **Aliases:** `iter.zip`

Combines two collections into a list of paired tuples.

