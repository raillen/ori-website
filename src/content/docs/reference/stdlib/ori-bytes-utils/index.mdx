---
title: "ori.bytes.utils"
description: "Byte buffer utility helpers. Provides convenience wrappers for common byte operations."
---

Byte buffer utility helpers. Provides convenience wrappers for common byte operations.

### `contains`

```ori
func contains(value: bytes, part: bytes) -> bool
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Returns true if the collection contains the specified element.

### `empty_bytes`

```ori
func empty_bytes() -> bytes
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Performs the empty bytes operation on utils.

### `ends_with`

```ori
func ends_with(value: bytes, suffix: bytes) -> bool
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Returns true if the string ends with the given suffix.

### `equals`

```ori
func equals(a: bytes, b: bytes) -> bool
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Performs the equals operation on utils.

### `from_hex_or`

```ori
func from_hex_or(hex: string, fallback: bytes) -> bytes
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Performs the from hex or operation on utils.

### `from_list`

```ori
func from_list(values: list<int>) -> bytes
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Creates a new collection initialized with the elements from the given list.

### `is_empty`

```ori
func is_empty(b: bytes) -> bool
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Returns true if the collection contains no elements.

### `join`

```ori
func join(left: bytes, right: bytes) -> bytes
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Joins a list of strings with the given separator.

### `starts_with`

```ori
func starts_with(value: bytes, prefix: bytes) -> bool
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Returns true if the string starts with the given prefix.

### `to_list`

```ori
func to_list(value: bytes) -> list<int>
```

*Layer 2 · .orl* · **Source:** `bytes/utils.orl`

Converts the collection into a plain list.

