---
title: "ori.tree"
description: "Standard library module `ori.tree`."
---

Standard library module `ori.tree`.

### `add_child`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId, arg2: _#0) -> tree.NodeId
```

*Layer 1 · runtime* · **Aliases:** `tree.add_child`

### `breadth_first`

```ori
(arg0: tree.Tree<_#0>) -> list<tree.NodeId>
```

*Layer 1 · runtime* · **Aliases:** `tree.breadth_first`

### `children`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId) -> list<tree.NodeId>
```

*Layer 1 · runtime* · **Aliases:** `tree.children`

### `clone`

```ori
(arg0: tree.Tree<_#0>) -> tree.Tree<_#0>
```

*Layer 1 · runtime* · **Aliases:** `tree.clone`

### `clone_subtree`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId) -> tree.Tree<_#0>
```

*Layer 1 · runtime* · **Aliases:** `tree.clone_subtree`

### `contains_node`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId) -> bool
```

*Layer 1 · runtime* · **Aliases:** `tree.contains_node`

### `depth`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId) -> int
```

*Layer 1 · runtime* · **Aliases:** `tree.depth`

### `find`

```ori
(arg0: tree.Tree<_#0>, arg1: _#0) -> optional<tree.NodeId>
```

*Layer 1 · runtime* · **Aliases:** `tree.find`

### `len`

```ori
(arg0: tree.Tree<_#0>) -> int
```

*Layer 1 · runtime* · **Aliases:** `tree.len`

### `move_subtree`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId, arg2: tree.NodeId) -> bool
```

*Layer 1 · runtime* · **Aliases:** `tree.move_subtree`

### `new`

```ori
(arg0: _#0) -> tree.Tree<_#0>
```

*Layer 1 · runtime* · **Aliases:** `tree.new`

### `parent`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId) -> optional<tree.NodeId>
```

*Layer 1 · runtime* · **Aliases:** `tree.parent`

### `post_order`

```ori
(arg0: tree.Tree<_#0>) -> list<tree.NodeId>
```

*Layer 1 · runtime* · **Aliases:** `tree.post_order`

### `pre_order`

```ori
(arg0: tree.Tree<_#0>) -> list<tree.NodeId>
```

*Layer 1 · runtime* · **Aliases:** `tree.pre_order`

### `remove_subtree`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId) -> void
```

*Layer 1 · runtime* · **Aliases:** `tree.remove_subtree`

### `root`

```ori
(arg0: tree.Tree<_#0>) -> tree.NodeId
```

*Layer 1 · runtime* · **Aliases:** `tree.root`

### `set_value`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId, arg2: _#0) -> bool
```

*Layer 1 · runtime* · **Aliases:** `tree.set_value`

### `try_value`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId) -> optional<_#0>
```

*Layer 1 · runtime* · **Aliases:** `tree.try_value`

### `value`

```ori
(arg0: tree.Tree<_#0>, arg1: tree.NodeId) -> _#0
```

*Layer 1 · runtime* · **Aliases:** `tree.value`

