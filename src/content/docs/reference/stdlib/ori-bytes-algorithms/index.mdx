---
title: "ori.bytes.algorithms"
description: "Byte buffer comparison and prefix-checking algorithms."
---

Byte buffer comparison and prefix-checking algorithms.

### `compare_lex`

```ori
func compare_lex(a: bytes, b: bytes) -> int
```

*Layer 2 · .orl* · **Source:** `bytes/algorithms.orl`

Performs the compare lex operation on algorithms.

### `is_prefix_of`

```ori
func is_prefix_of(prefix: bytes, value: bytes) -> bool
```

*Layer 2 · .orl* · **Source:** `bytes/algorithms.orl`

Performs the is prefix of operation on algorithms.

