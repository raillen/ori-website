---
title: "ori.string.utils"
description: "String utility helpers. Provides additional string operations for case conversion, counting, padding, and text analysis."
---

String utility helpers. Provides additional string operations for case conversion, counting, padding, and text analysis.

### `blank`

```ori
func blank(s: string) -> bool
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Returns true if the string is empty or contains only whitespace.

### `capitalize`

```ori
func capitalize(s: string) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the capitalize operation on utils.

### `center`

```ori
func center(s: string, width: int) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the center operation on utils.

### `count`

```ori
func count(s: string, sub: string) -> int
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the count operation on utils.

### `default`

```ori
func default(s: string, fallback: string) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the default operation on utils.

### `equals_ignore_case`

```ori
func equals_ignore_case(a: string, b: string) -> bool
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the equals ignore case operation on utils.

### `has_prefix`

```ori
func has_prefix(s: string, prefix: string) -> bool
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the has prefix operation on utils.

### `has_suffix`

```ori
func has_suffix(s: string, suffix: string) -> bool
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the has suffix operation on utils.

### `has_whitespace`

```ori
func has_whitespace(s: string) -> bool
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the has whitespace operation on utils.

### `is_digits`

```ori
func is_digits(s: string) -> bool
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the is digits operation on utils.

### `is_empty`

```ori
func is_empty(s: string) -> bool
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Returns true if the collection contains no elements.

### `last_index_of`

```ori
func last_index_of(s: string, needle: string) -> int
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the last index of operation on utils.

### `left`

```ori
func left(s: string, n: int) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the left operation on utils.

### `limit`

```ori
func limit(s: string, max_len: int) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the limit operation on utils.

### `lines`

```ori
func lines(s: string) -> list<string>
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the lines operation on utils.

### `replace_all`

```ori
func replace_all(s: string, needle: string, replacement: string) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the replace all operation on utils.

### `replicate`

```ori
func replicate(s: string, n: int) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the replicate operation on utils.

### `reverse`

```ori
func reverse(s: string) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Returns a new collection with elements in reversed order.

### `right`

```ori
func right(s: string, n: int) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the right operation on utils.

### `swap_case`

```ori
func swap_case(s: string) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the swap case operation on utils.

### `title`

```ori
func title(s: string) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the title operation on utils.

### `trim_all`

```ori
func trim_all(s: string) -> string
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the trim all operation on utils.

### `words`

```ori
func words(s: string) -> list<string>
```

*Layer 2 · .orl* · **Source:** `string/utils.orl`

Performs the words operation on utils.

