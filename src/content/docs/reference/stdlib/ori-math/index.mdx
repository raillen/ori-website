---
title: ori.math
description: Standard library module ori.math
---

# ori.math

### `ori.math.abs`

```ori
(arg0: int) -> int
```

- Layer 1 · runtime

### `ori.math.acos`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime
  - Aliases: `math.acos`

### `ori.math.asin`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime
  - Aliases: `math.asin`

### `ori.math.atan`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime
  - Aliases: `math.atan`

### `ori.math.atan2`

```ori
(arg0: float, arg1: float) -> float
```

- Layer 1 · runtime
  - Aliases: `math.atan2`

### `ori.math.ceil`

```ori
(arg0: float) -> int
```

- Layer 1 · runtime

### `ori.math.clamp`

```ori
(arg0: int, arg1: int, arg2: int) -> int
```

- Layer 1 · runtime

### `ori.math.cos`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime

### `ori.math.exp`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime
  - Aliases: `math.exp`

### `ori.math.floor`

```ori
(arg0: float) -> int
```

- Layer 1 · runtime

### `ori.math.is_finite`

```ori
(arg0: float) -> bool
```

- Layer 1 · runtime
  - Aliases: `math.is_finite`

### `ori.math.is_infinite`

```ori
(arg0: float) -> bool
```

- Layer 1 · runtime

### `ori.math.is_nan`

```ori
(arg0: float) -> bool
```

- Layer 1 · runtime

### `ori.math.ln`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime
  - Aliases: `math.ln`

### `ori.math.log`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime

### `ori.math.log10`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime
  - Aliases: `math.log10`

### `ori.math.log2`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime

### `ori.math.max`

```ori
(arg0: int, arg1: int) -> int
```

- Layer 1 · runtime

### `ori.math.min`

```ori
(arg0: int, arg1: int) -> int
```

- Layer 1 · runtime

### `ori.math.pow`

```ori
(arg0: float, arg1: float) -> float
```

- Layer 1 · runtime

### `ori.math.round`

```ori
(arg0: float) -> int
```

- Layer 1 · runtime

### `ori.math.sin`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime

### `ori.math.sqrt`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime

### `ori.math.tan`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime

### `ori.math.trunc`

```ori
(arg0: float) -> float
```

- Layer 1 · runtime
  - Aliases: `math.trunc`

