---
title: "ori.os.utils"
description: "OS utility helpers. Provides convenience wrappers with fallback defaults for platform and environment queries."
---

OS utility helpers. Provides convenience wrappers with fallback defaults for platform and environment queries.

### `arch`

```ori
func arch() -> string
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Returns the CPU architecture (e.g., x86_64, aarch64).

### `args`

```ori
func args() -> list<string>
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Returns the command-line arguments passed to the program.

### `current_dir_or`

```ori
func current_dir_or(fallback: string) -> string
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Performs the current dir or operation on utils.

### `env_or`

```ori
func env_or(name: string, fallback: string) -> string
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Performs the env or operation on utils.

### `has_env`

```ori
func has_env(name: string) -> bool
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Performs the has env operation on utils.

### `is_linux`

```ori
func is_linux() -> bool
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Performs the is linux operation on utils.

### `is_macos`

```ori
func is_macos() -> bool
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Performs the is macos operation on utils.

### `is_windows`

```ori
func is_windows() -> bool
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Performs the is windows operation on utils.

### `pid`

```ori
func pid() -> int
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Returns the current process ID.

### `platform`

```ori
func platform() -> string
```

*Layer 2 · .orl* · **Source:** `os/utils.orl`

Returns the current operating system platform name.

