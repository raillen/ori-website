---
title: "ori.map"
description: "Hash map (key-value dictionary) utilities for storing, retrieving, and checking keys."
---

Hash map (key-value dictionary) utilities for storing, retrieving, and checking keys.

### `capacity`

```ori
(arg0: map<_#0, _#1>) -> int
```

*Layer 1 · runtime* · **Aliases:** `map.capacity`

Returns the current allocated capacity of the collection.

### `clear`

```ori
(arg0: map<_#0, _#1>) -> void
```

*Layer 1 · runtime* · **Aliases:** `map.clear`

Removes all elements from the collection, resetting it to empty.

### `clone`

```ori
(arg0: map<_#0, _#1>) -> map<_#0, _#1>
```

*Layer 1 · runtime* · **Aliases:** `map.clone`

Returns a deep copy of the collection, independent of the original.

### `contains`

```ori
(arg0: map<_#0, _#1>, arg1: _#0) -> bool
```

*Layer 1 · runtime* · **Aliases:** `map.contains`

Returns true if the collection contains the specified element.

### `entries`

```ori
(arg0: map<_#0, _#1>) -> list<tuple<_#0, _#1>>
```

*Layer 1 · runtime* · **Aliases:** `map.entries`

Returns all key-value pairs (or index-value pairs) as a list of tuples.

### `free`

```ori
(arg0: map<_#0, _#1>) -> void
```

*Layer 1 · runtime* · **Aliases:** `map.free`

Releases the underlying memory allocated by the collection.

### `from_entries`

```ori
(arg0: list<tuple<_#0, _#1>>) -> map<_#0, _#1>
```

*Layer 1 · runtime* · **Aliases:** `map.from_entries`

Creates a new collection from a list of key-value pairs.

### `get`

```ori
(arg0: map<_#0, _#1>, arg1: _#0) -> _#1
```

*Layer 1 · runtime* · **Aliases:** `map.get`

Returns the element at the specified index without removing it.

### `is_empty`

```ori
(arg0: map<_#0, _#1>) -> bool
```

*Layer 1 · runtime* · **Aliases:** `map.is_empty`

Returns true if the collection contains no elements.

### `keys`

```ori
(arg0: map<_#0, _#1>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `map.keys`

Returns a list of all keys in the collection.

### `len`

```ori
(arg0: map<_#0, _#1>) -> int
```

*Layer 1 · runtime* · **Aliases:** `map.len`

Returns the number of elements in the collection.

### `new`

```ori
() -> map<_#0, _#1>
```

*Layer 1 · runtime* · **Aliases:** `map.new`

Creates a new empty instance of the map collection.

### `remove`

```ori
(arg0: map<_#0, _#1>, arg1: _#0) -> void
```

*Layer 1 · runtime* · **Aliases:** `map.remove`

Removes and returns the element at the specified index.

### `reserve`

```ori
(arg0: map<_#0, _#1>, arg1: int) -> void
```

*Layer 1 · runtime* · **Aliases:** `map.reserve`

Ensures the collection has capacity for at least the specified number of elements.

### `set`

```ori
(arg0: map<_#0, _#1>, arg1: _#0, arg2: _#1) -> void
```

*Layer 1 · runtime* · **Aliases:** `map.set`

Replaces the element at the specified index with a new value.

### `try_get`

```ori
(arg0: map<_#0, _#1>, arg1: _#0) -> optional<_#1>
```

*Layer 1 · runtime* · **Aliases:** `map.try_get`

Returns the element at the specified index, or an error if the index is out of bounds.

### `try_remove`

```ori
(arg0: map<_#0, _#1>, arg1: _#0) -> optional<_#1>
```

*Layer 1 · runtime* · **Aliases:** `map.try_remove`

Removes the element at the specified index, or returns an error if out of bounds.

### `values`

```ori
(arg0: map<_#0, _#1>) -> list<_#1>
```

*Layer 1 · runtime* · **Aliases:** `map.values`

Returns a list of all values in the collection.

