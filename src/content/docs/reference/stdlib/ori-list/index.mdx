---
title: "ori.list"
description: "List manipulation core module. Houses size checks, indexing operations, pushing, popping, and slice manipulations."
---

List manipulation core module. Houses size checks, indexing operations, pushing, popping, and slice manipulations.

### `clear`

```ori
(arg0: list<_#0>) -> void
```

*Layer 1 · runtime* · **Aliases:** `list.clear`

Removes all elements from the collection, resetting it to empty.

### `clone`

```ori
(arg0: list<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `list.clone`

Returns a deep copy of the collection, independent of the original.

### `contains`

```ori
(arg0: list<_#0>, arg1: _#0) -> bool
```

*Layer 1 · runtime* · **Aliases:** `list.contains`

Returns true if the collection contains the specified element.

### `filter`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> bool) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `list.filter`

Returns a new collection containing only elements that satisfy the given predicate.

### `free`

```ori
(arg0: list<_#0>) -> void
```

*Layer 1 · runtime* · **Aliases:** `list.free`

Releases the underlying memory allocated by the collection.

### `from_list`

```ori
(arg0: list<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `list.from_list`

Creates a new collection initialized with the elements from the given list.

### `get`

```ori
(arg0: list<_#0>, arg1: int) -> _#0
```

*Layer 1 · runtime* · **Aliases:** `list.get`

Returns the element at the specified index without removing it.

### `index_of`

```ori
(arg0: list<_#0>, arg1: _#0) -> int
```

*Layer 1 · runtime* · **Aliases:** `list.index_of`

Returns the index of the first occurrence of the given element, or -1 if not found.

### `insert`

```ori
(arg0: list<_#0>, arg1: int, arg2: _#0) -> void
```

*Layer 1 · runtime* · **Aliases:** `list.insert`

Inserts an element at the specified index, shifting subsequent elements.

### `is_empty`

```ori
(arg0: list<_#0>) -> bool
```

*Layer 1 · runtime* · **Aliases:** `list.is_empty`

Returns true if the collection contains no elements.

### `len`

```ori
(arg0: list<_#0>) -> int
```

*Layer 1 · runtime* · **Aliases:** `list.len`

Returns the number of elements in the collection.

### `map`

```ori
(arg0: list<_#0>, arg1: func(_#0) -> _#1) -> list<_#1>
```

*Layer 1 · runtime* · **Aliases:** `list.map`

Returns a new collection by applying a transformation function to each element.

### `new`

```ori
() -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `list.new`

Creates a new empty instance of the list collection.

### `pop`

```ori
(arg0: list<_#0>) -> _#0
```

*Layer 1 · runtime* · **Aliases:** `list.pop`

Removes and returns the last element from the collection.

### `push`

```ori
(arg0: list<_#0>, arg1: _#0) -> void
```

*Layer 1 · runtime* · **Aliases:** `list.push`

Adds an element to the collection.

### `remove`

```ori
(arg0: list<_#0>, arg1: int) -> void
```

*Layer 1 · runtime* · **Aliases:** `list.remove`

Removes and returns the element at the specified index.

### `reverse`

```ori
(arg0: list<_#0>) -> void
```

*Layer 1 · runtime* · **Aliases:** `list.reverse`

Returns a new collection with elements in reversed order.

### `set`

```ori
(arg0: list<_#0>, arg1: int, arg2: _#0) -> void
```

*Layer 1 · runtime* · **Aliases:** `list.set`

Replaces the element at the specified index with a new value.

### `slice`

```ori
(arg0: list<_#0>, arg1: int, arg2: int) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `list.slice`

Returns a new collection containing elements from the specified start to end index.

### `sort`

```ori
(arg0: list<_#0>) -> void
```

*Layer 1 · runtime* · **Aliases:** `list.sort`

Returns a new collection with elements sorted in ascending order.

### `to_list`

```ori
(arg0: list<_#0>) -> list<_#0>
```

*Layer 1 · runtime* · **Aliases:** `list.to_list`

Converts the collection into a plain list.

### `try_get`

```ori
(arg0: list<_#0>, arg1: int) -> optional<_#0>
```

*Layer 1 · runtime* · **Aliases:** `list.try_get`

Returns the element at the specified index, or an error if the index is out of bounds.

### `try_pop`

```ori
(arg0: list<_#0>) -> optional<_#0>
```

*Layer 1 · runtime* · **Aliases:** `list.try_pop`

Removes and returns the last element, or returns an error if empty.

### `try_remove`

```ori
(arg0: list<_#0>, arg1: int) -> bool
```

*Layer 1 · runtime* · **Aliases:** `list.try_remove`

Removes the element at the specified index, or returns an error if out of bounds.

