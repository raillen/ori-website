---
title: "ori.string.algorithms"
description: "String algorithms. Provides comparison, joining, repetition, and truncation operations."
---

String algorithms. Provides comparison, joining, repetition, and truncation operations.

### `equals_any`

```ori
func equals_any(value: string, candidates: list<string>) -> bool
```

*Layer 2 · .orl* · **Source:** `string/algorithms.orl`

Performs the equals any operation on algorithms.

### `join_non_empty`

```ori
func join_non_empty(parts: list<string>, separator: string) -> string
```

*Layer 2 · .orl* · **Source:** `string/algorithms.orl`

Performs the join non empty operation on algorithms.

### `repeated`

```ori
func repeated(s: string, times: int) -> string
```

*Layer 2 · .orl* · **Source:** `string/algorithms.orl`

Performs the repeated operation on algorithms.

### `truncate`

```ori
func truncate(s: string, max_len: int) -> string
```

*Layer 2 · .orl* · **Source:** `string/algorithms.orl`

Performs the truncate operation on algorithms.

