---
title: "ori.iter.utils"
description: "Iterator utility helpers. Provides convenience functions for common iterator patterns on int sequences."
---

Iterator utility helpers. Provides convenience functions for common iterator patterns on int sequences.

### `all_equal_int`

```ori
func all_equal_int(items: list<int>, expected: int) -> bool
```

*Layer 2 · .orl* · **Source:** `iter/utils.orl`

Performs the all equal int operation on utils.

### `contains_int`

```ori
func contains_int(items: list<int>, target: int) -> bool
```

*Layer 2 · .orl* · **Source:** `iter/utils.orl`

Performs the contains int operation on utils.

### `count`

```ori
func count(items: list<T>) -> int
```

*Layer 2 · .orl* · **Source:** `iter/utils.orl`

Performs the count operation on utils.

### `first`

```ori
func first(items: list<T>) -> optional<T>
```

*Layer 2 · .orl* · **Source:** `iter/utils.orl`

Performs the first operation on utils.

### `is_empty`

```ori
func is_empty(items: list<T>) -> bool
```

*Layer 2 · .orl* · **Source:** `iter/utils.orl`

Returns true if the collection contains no elements.

### `reversed_copy`

```ori
func reversed_copy(items: list<T>) -> list<T>
```

*Layer 2 · .orl* · **Source:** `iter/utils.orl`

Performs the reversed copy operation on utils.

### `sum_int`

```ori
func sum_int(items: list<int>) -> int
```

*Layer 2 · .orl* · **Source:** `iter/utils.orl`

Performs the sum int operation on utils.

### `unique_count_int`

```ori
func unique_count_int(items: list<int>) -> int
```

*Layer 2 · .orl* · **Source:** `iter/utils.orl`

Performs the unique count int operation on utils.

