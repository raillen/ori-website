---
title: "ori.time.utils"
description: "Time utility helpers. Provides arithmetic operations on durations and convenience functions for time conversion."
---

Time utility helpers. Provides arithmetic operations on durations and convenience functions for time conversion.

### `add`

```ori
func add(at: int, duration: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Adds an element to the set. Returns true if the element was newly inserted.

### `diff`

```ori
func diff(a: int, b: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the diff operation on utils.

### `from_unix_ms`

```ori
func from_unix_ms(ts: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the from unix ms operation on utils.

### `hours`

```ori
func hours(n: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the hours operation on utils.

### `milliseconds`

```ori
func milliseconds(n: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the milliseconds operation on utils.

### `minutes`

```ori
func minutes(n: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the minutes operation on utils.

### `seconds`

```ori
func seconds(n: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the seconds operation on utils.

### `since`

```ori
func since(start: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the since operation on utils.

### `sleep_ms`

```ori
func sleep_ms(duration: int)
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the sleep ms operation on utils.

### `sub`

```ori
func sub(at: int, duration: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the sub operation on utils.

### `to_unix_ms`

```ori
func to_unix_ms(at: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the to unix ms operation on utils.

### `until`

```ori
func until(target: int) -> int
```

*Layer 2 · .orl* · **Source:** `time/utils.orl`

Performs the until operation on utils.

