---
title: "ori.random.utils"
description: "Random utility helpers. Provides additional random generation functions including seeded generators and collection shuffling."
---

Random utility helpers. Provides additional random generation functions including seeded generators and collection shuffling.

### `coin`

```ori
func coin() -> bool
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the coin operation on utils.

### `float_range`

```ori
func float_range(min_val: float, max_val: float) -> float
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the float range operation on utils.

### `int_range`

```ori
func int_range(min_val: int, max_val: int) -> int
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the int range operation on utils.

### `is_empty_choice`

```ori
func is_empty_choice(items: list<string>) -> bool
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the is empty choice operation on utils.

### `pick_int`

```ori
func pick_int(items: list<int>) -> optional<int>
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the pick int operation on utils.

### `pick_string`

```ori
func pick_string(items: list<string>) -> optional<string>
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the pick string operation on utils.

### `seeded_int`

```ori
func seeded_int(seed: int, min_val: int, max_val: int) -> int
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the seeded int operation on utils.

### `seeded_pick_string`

```ori
func seeded_pick_string(seed: int, items: list<string>) -> optional<string>
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the seeded pick string operation on utils.

### `shuffle_ints`

```ori
func shuffle_ints(items: list<int>) -> list<int>
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the shuffle ints operation on utils.

### `shuffle_strings`

```ori
func shuffle_strings(items: list<string>) -> list<string>
```

*Layer 2 · .orl* · **Source:** `random/utils.orl`

Performs the shuffle strings operation on utils.

