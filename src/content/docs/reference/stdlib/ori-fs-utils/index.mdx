---
title: "ori.fs.utils"
description: "File system utility helpers. Provides convenience wrappers with fallback defaults for common file operations."
---

File system utility helpers. Provides convenience wrappers with fallback defaults for common file operations.

### `create_dir_all`

```ori
func create_dir_all(path: string) -> result<void, string>
```

*Layer 2 · .orl* · **Source:** `fs/utils.orl`

Creates a directory and all necessary parent directories.

### `exists_result`

```ori
func exists_result(path: string) -> result<bool, string>
```

*Layer 2 · .orl* · **Source:** `fs/utils.orl`

Performs the exists result operation on utils.

### `move_path`

```ori
func move_path(from: string, to: string) -> result<void, string>
```

*Layer 2 · .orl* · **Source:** `fs/utils.orl`

Performs the move path operation on utils.

### `read_text_or`

```ori
func read_text_or(path: string, fallback: string) -> string
```

*Layer 2 · .orl* · **Source:** `fs/utils.orl`

Performs the read text or operation on utils.

### `remove_file`

```ori
func remove_file(path: string) -> result<void, string>
```

*Layer 2 · .orl* · **Source:** `fs/utils.orl`

Performs the remove file operation on utils.

### `write_text_result`

```ori
func write_text_result(path: string, content: string) -> result<string, string>
```

*Layer 2 · .orl* · **Source:** `fs/utils.orl`

Performs the write text result operation on utils.

