---
title: "ori.set.algorithms"
description: "Set algorithms. Provides batch operations like union, intersection, difference, and disjoint checks on typed sets."
---

Set algorithms. Provides batch operations like union, intersection, difference, and disjoint checks on typed sets.

### `difference_string`

```ori
func difference_string(a: set<string>, b: set<string>) -> set<string>
```

*Layer 2 · .orl* · **Source:** `set/algorithms.orl`

Performs the difference string operation on algorithms.

### `intersection_int`

```ori
func intersection_int(a: set<int>, b: set<int>) -> set<int>
```

*Layer 2 · .orl* · **Source:** `set/algorithms.orl`

Performs the intersection int operation on algorithms.

### `intersection_size_string`

```ori
func intersection_size_string(a: set<string>, b: set<string>) -> int
```

*Layer 2 · .orl* · **Source:** `set/algorithms.orl`

Performs the intersection size string operation on algorithms.

### `intersection_string`

```ori
func intersection_string(a: set<string>, b: set<string>) -> set<string>
```

*Layer 2 · .orl* · **Source:** `set/algorithms.orl`

Performs the intersection string operation on algorithms.

### `is_disjoint_string`

```ori
func is_disjoint_string(a: set<string>, b: set<string>) -> bool
```

*Layer 2 · .orl* · **Source:** `set/algorithms.orl`

Performs the is disjoint string operation on algorithms.

### `union_int`

```ori
func union_int(a: set<int>, b: set<int>) -> set<int>
```

*Layer 2 · .orl* · **Source:** `set/algorithms.orl`

Performs the union int operation on algorithms.

### `union_string`

```ori
func union_string(a: set<string>, b: set<string>) -> set<string>
```

*Layer 2 · .orl* · **Source:** `set/algorithms.orl`

Performs the union string operation on algorithms.

