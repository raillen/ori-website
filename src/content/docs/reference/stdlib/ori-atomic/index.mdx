---
title: ori.atomic
description: Standard library module `ori.atomic`.
---

# ori.atomic

Standard library module `ori.atomic`.

### `ori.atomic.add`

```ori
(arg0: atomic.AtomicInt, arg1: int) -> int
```

- Layer 1 · runtime
  - Aliases: `atomic.add`

### `ori.atomic.load`

```ori
(arg0: atomic.AtomicInt) -> int
```

- Layer 1 · runtime
  - Aliases: `atomic.load`

### `ori.atomic.new`

```ori
(arg0: int) -> atomic.AtomicInt
```

- Layer 1 · runtime
  - Aliases: `atomic.new`

### `ori.atomic.store`

```ori
(arg0: atomic.AtomicInt, arg1: int) -> void
```

- Layer 1 · runtime
  - Aliases: `atomic.store`

