---
title: "ori.concurrent.utils"
description: "Concurrency utility helpers. Provides typed copy and transfer operations for concurrent-safe values."
---

Concurrency utility helpers. Provides typed copy and transfer operations for concurrent-safe values.

### `copy_bool`

```ori
func copy_bool(value: bool) -> bool
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy bool operation on utils.

### `copy_bytes`

```ori
func copy_bytes(value: bytes) -> bytes
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy bytes operation on utils.

### `copy_float`

```ori
func copy_float(value: float) -> float
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy float operation on utils.

### `copy_int`

```ori
func copy_int(value: int) -> int
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy int operation on utils.

### `copy_list_int`

```ori
func copy_list_int(value: list<int>) -> list<int>
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy list int operation on utils.

### `copy_list_string`

```ori
func copy_list_string(value: list<string>) -> list<string>
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy list string operation on utils.

### `copy_map_string_string`

```ori
func copy_map_string_string(value: map<string, string>) -> map<string, string>
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy map string string operation on utils.

### `copy_set_string`

```ori
func copy_set_string(value: set<string>) -> set<string>
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy set string operation on utils.

### `copy_string`

```ori
func copy_string(value: string) -> string
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the copy string operation on utils.

### `transfer_int`

```ori
func transfer_int(value: int) -> int
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the transfer int operation on utils.

### `transfer_list_string`

```ori
func transfer_list_string(value: list<string>) -> list<string>
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the transfer list string operation on utils.

### `transfer_string`

```ori
func transfer_string(value: string) -> string
```

*Layer 2 · .orl* · **Source:** `concurrent/utils.orl`

Performs the transfer string operation on utils.

