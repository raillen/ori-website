---
title: ori.io
description: Standard Input/Output utilities. Provides functions to read from and write to the terminal console.
---

# ori.io

Standard Input/Output utilities. Provides functions to read from and write to the terminal console.

### `ori.io.eprint`

```ori
(arg0: string) -> void
```

- Layer 1 · runtime

### `ori.io.eprintln`

```ori
(arg0: string) -> void
```

- Layer 1 · runtime

### `ori.io.print`

```ori
(arg0: string) -> void
```

- Layer 1 · runtime

Writes a string to the standard output without appending a newline.

**Applicability:**
Used for printing prompt messages, building progress indicators, or custom formatted output.

```ori
import ori.io as io

func main()
    io.print("Enter value: ")
end
```

### `ori.io.println`

```ori
(arg0: string) -> void
```

- Layer 1 · runtime

Writes a string to the standard output, followed by a newline.

**Applicability:**
Used for printing log messages, general command line outputs, and standard debugging text.

```ori
import ori.io as io

func main()
    io.println("Application started successfully.")
end
```

### `ori.io.read_line`

```ori
() -> optional<string>
```

- Layer 1 · runtime

