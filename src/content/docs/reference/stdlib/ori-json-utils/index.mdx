---
title: "ori.json.utils"
description: "JSON utility helpers. Provides convenience wrappers for reading and writing JSON files."
---

JSON utility helpers. Provides convenience wrappers for reading and writing JSON files.

### `read`

```ori
func read(file_path: string) -> result<json.Value, string>
```

*Layer 2 · .orl* · **Source:** `json/utils.orl`

Reads the contents of a file into a bytes buffer.

### `write`

```ori
func write(file_path: string, value: json.Value) -> result<string, string>
```

*Layer 2 · .orl* · **Source:** `json/utils.orl`

Writes a bytes buffer to a file.

### `write_pretty`

```ori
func write_pretty(file_path: string, value: json.Value) -> result<string, string>
```

*Layer 2 · .orl* · **Source:** `json/utils.orl`

Performs the write pretty operation on utils.

