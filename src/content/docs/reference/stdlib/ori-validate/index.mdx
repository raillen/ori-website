---
title: "ori.validate"
description: "Input validation primitives. Provides predicates for checking ranges, emptiness, lengths, and custom conditions."
---

Input validation primitives. Provides predicates for checking ranges, emptiness, lengths, and custom conditions.

### `all_digits`

```ori
func all_digits(value: string) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string contains only digit characters.

### `between`

```ori
func between(value: int, min_val: int, max_val: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the value is between the specified min and max (exclusive).

### `blank`

```ori
func blank(value: string) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string is empty or contains only whitespace.

### `even`

```ori
func even(value: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the integer is even.

### `in_range`

```ori
func in_range(value: int, min_val: int, max_val: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the value is within the specified range (inclusive).

### `int_in_range`

```ori
func int_in_range(value: int, min_val: int, max_val: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the integer is within the specified range (inclusive).

### `length_between`

```ori
func length_between(value: string, min_val: int, max_val: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string length is between the specified min and max.

### `length_equal`

```ori
func length_equal(value: string, expected: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string length equals the specified value.

### `matches_length`

```ori
func matches_length(value: string, exact: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string length matches the specified value.

### `max_len`

```ori
func max_len(value: string, max_val: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string has at most the maximum length.

### `max_length`

```ori
func max_length(value: string, max_val: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string has at most the maximum length.

### `min_len`

```ori
func min_len(value: string, min_val: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string has at least the minimum length.

### `min_length`

```ori
func min_length(value: string, min_val: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string has at least the minimum length.

### `negative`

```ori
func negative(value: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the number is less than zero.

### `non_negative`

```ori
func non_negative(value: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the number is greater than or equal to zero.

### `non_zero`

```ori
func non_zero(value: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the number is not zero.

### `not_empty`

```ori
func not_empty(value: string) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the collection is not empty.

### `not_empty_text`

```ori
func not_empty_text(value: string) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string is not empty.

### `odd`

```ori
func odd(value: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the integer is odd.

### `one_of`

```ori
func one_of(value: int, candidates: list<int>) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the value matches one of the allowed values.

### `one_of_string`

```ori
func one_of_string(value: string, candidates: list<string>) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string equals one of the allowed values.

### `positive`

```ori
func positive(value: int) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the number is greater than zero.

### `string_not_blank`

```ori
func string_not_blank(value: string) -> bool
```

*Layer 2 · .orl* · **Source:** `validate.orl`

Returns true if the string is not blank.

