---
title: ori.map.utils
description: Standard library module ori.map.utils
---

# ori.map.utils

### `ori.map.utils.contains_key`

```ori
func contains_key(m: map<string, int>, key: string) -> bool
```

- Layer 2 · .orl
  - Source: `map/utils.orl`

### `ori.map.utils.get_or`

```ori
func get_or(m: map<string, int>, key: string, fallback: int) -> int
```

- Layer 2 · .orl
  - Source: `map/utils.orl`

### `ori.map.utils.get_or_string`

```ori
func get_or_string(m: map<string, string>, key: string, fallback: string) -> string
```

- Layer 2 · .orl
  - Source: `map/utils.orl`

### `ori.map.utils.has_key`

```ori
func has_key(m: map<string, int>, key: string) -> bool
```

- Layer 2 · .orl
  - Source: `map/utils.orl`

### `ori.map.utils.is_empty`

```ori
func is_empty(m: map<string, int>) -> bool
```

- Layer 2 · .orl
  - Source: `map/utils.orl`

