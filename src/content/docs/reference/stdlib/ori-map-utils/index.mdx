---
title: "ori.map.utils"
description: "Map utility helpers. Provides convenience accessors with fallback defaults for map lookups."
---

Map utility helpers. Provides convenience accessors with fallback defaults for map lookups.

### `contains_key`

```ori
func contains_key(m: map<string, int>, key: string) -> bool
```

*Layer 2 · .orl* · **Source:** `map/utils.orl`

Performs the contains key operation on utils.

### `get_or`

```ori
func get_or(m: map<string, int>, key: string, fallback: int) -> int
```

*Layer 2 · .orl* · **Source:** `map/utils.orl`

Performs the get or operation on utils.

### `get_or_string`

```ori
func get_or_string(m: map<string, string>, key: string, fallback: string) -> string
```

*Layer 2 · .orl* · **Source:** `map/utils.orl`

Performs the get or string operation on utils.

### `has_key`

```ori
func has_key(m: map<string, int>, key: string) -> bool
```

*Layer 2 · .orl* · **Source:** `map/utils.orl`

Performs the has key operation on utils.

### `is_empty`

```ori
func is_empty(m: map<string, int>) -> bool
```

*Layer 2 · .orl* · **Source:** `map/utils.orl`

Returns true if the collection contains no elements.

